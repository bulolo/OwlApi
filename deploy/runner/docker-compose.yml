# OwlApi Runner 部署配置
# 
# 使用方法：
# 1. 复制此文件到您的服务器
# 2. 修改环境变量配置
# 3. 运行: docker compose up -d
#
# 获取 RUNNER_ID 和 RUNNER_TOKEN：
# 登录 OwlApi 控制台 -> 项目设置 -> 网关节点管理 -> 创建执行节点

services:
  runner:
    build:
      context: ../../backend
      dockerfile: Dockerfile.runner
    image: registry.cn-hangzhou.aliyuncs.com/owlapi/runner:latest
    container_name: owlapi_runner
    restart: unless-stopped
    environment:
      # 必填：OwlApi 服务器地址
      - OWLAPI_SERVER_URL=https://api.owlapi.io
      # 必填：在控制台创建网关后获取
      - OWLAPI_RUNNER_ID=your-runner-id
      - OWLAPI_RUNNER_TOKEN=your-runner-token
      # 可选：日志级别 (debug, info, warn, error)
      - OWLAPI_LOG_LEVEL=info
      # 时区设置
      - TZ=Asia/Shanghai
    volumes:
      # 日志持久化
      - ./logs:/owlapi/logs
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
    # 健康检查（可选，需要 Agent 支持）
    # healthcheck:
    #   test: ["CMD", "wget", "-q", "--spider", "http://localhost:8081/health"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
